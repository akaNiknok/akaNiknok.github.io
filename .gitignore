# src/ is local-only source notes — confidential Pareto material lives here
# (performance reports, Slack-derived metrics, HR docs). Nothing in src/ is
# part of the published site. Ignore the whole directory, not per-file patterns.
src/
